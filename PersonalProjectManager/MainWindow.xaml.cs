using System.Windows;

namespace PersonalProjectManager
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
            // DataContext được thiết lập trong App.xaml.cs
            // Không cần code gì thêm ở đây cho việc điều hướng cơ bản.
        }
    }
}
