<UserControl x:Class="PersonalProjectManager.Views.ProgressDashboardView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:PersonalProjectManager.Views"
                 xmlns:vm="clr-namespace:PersonalProjectManager.ViewModels"
                 xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:converters="clr-namespace:PersonalProjectManager.Converters"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance Type=vm:ProgressDashboardViewModel, IsDesignTimeCreatable=False}"
                 Background="White"
                 d:DesignHeight="600" d:DesignWidth="750">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Tổng Quan Tiến Độ Dự Án" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
            <Button Content="Làm Mới" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" VerticalAlignment="Center" ToolTip="Tải lại dữ liệu tiến độ."/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <ListView ItemsSource="{Binding ProjectSummaries}"
                          BorderThickness="0"
                          HorizontalContentAlignment="Stretch"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding ViewProjectDetailCommand}"
                                                   CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Path=Content}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ProjectSummaryItem}">
                        <Border Padding="10,8" BorderBrush="{StaticResource LightGrayBrush}" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding ProjectName}" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="1" Text="{Binding OverallProgress, StringFormat={}{0:N1} %}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="15,0"/>
                                <ProgressBar Grid.Column="2" Value="{Binding OverallProgress}" Maximum="100" Height="12" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>

        <TextBlock Grid.Row="2" Text="{Binding InfoMessage}"
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Margin="0,10,0,0" FontStyle="Italic" HorizontalAlignment="Center"
                        Visibility="{Binding InfoMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

        <Border Grid.Row="0" Grid.RowSpan="3" Background="#AAFFFFFF" CornerRadius="4"
                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Đang tải dữ liệu tiến độ..." FontSize="16" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                <ProgressBar IsIndeterminate="True" Height="10" Width="200" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
    