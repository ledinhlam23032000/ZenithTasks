<Window x:Class="PersonalProjectManager.Views.AddEditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonalProjectManager.Views"
        xmlns:vm="clr-namespace:PersonalProjectManager.ViewModels"
        xmlns:converters="clr-namespace:PersonalProjectManager.Converters"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=vm:AddEditTaskViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource BaseTextBoxStyle}"/>
        <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}"/>
        <Style x:Key="ErrorTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="Margin" Value="5,2,0,10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Visibility" Value="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Tên nhiệm vụ (*):" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding TaskName, UpdateSourceTrigger=PropertyChanged}" MaxLength="200"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Ghi chú:" VerticalAlignment="Top" Margin="0,10,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.RowSpan="2"
                 Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,10,0,0"
                 MinHeight="100"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding ErrorMessage}"
                   Style="{StaticResource ErrorTextBlockStyle}" Margin="0,5,0,5"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Lưu" Width="80" IsDefault="True" Click="SaveButton_Click"/>
            <Button Content="Hủy bỏ" Width="80" Margin="10,0,0,0" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>

    </Grid>
</Window>